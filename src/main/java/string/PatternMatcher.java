package string;

import java.util.regex.Pattern;

public class PatternMatcher {
/*PatternMatcher patternMatcher=new PatternMatcher();
patternMatcher.isMatch()

}
    int isMatch(char []pattern, char []input)
    {
        if (pattern==null || input==null) {
            return 0;
        } else if (pattern.length>1 && pattern[1] =='?') {
            return (pattern[0] == input[0] && is_match(pattern.s, input+1))
                    || is_match(pattern+2, input);
        } else if (pattern[1] == '*') {
            size_t i;
            for (i=0; input[i] == pattern[0]; i++)
                if (is_match(pattern+2, input+i)) return 1;
            return 0;
        } else {
            return pattern[0] == input[0] && is_match(pattern+1, input+1);
        }
*/
}
